# ai_sdk

Experimental internal AI SDK sample.

This project is a small, production-minded Python library that exposes a simple
`AIClient` class other services can import.

To be added:

- Langfuse tracing baked into the SDK
- Tests, DeepEval evaluation, and CI integration
- summarise model
